interface:
  display_name: "SIF 功能：查询某 ASIN 在历史各周内新上线的 campaign 变更事件，即 campaign_created 事件列表。"
  short_description: "调用 SIF standard API：功能：查询某 ASIN 在历史各周内新上线的 campaign 变更事件，即 campa"
  default_prompt: "Use $wenmai-sif-ads-get-asin-campaign-changes to call ads_get_asin_campaign_changes and return traceable results."
policy:
  allow_implicit_invocation: true
