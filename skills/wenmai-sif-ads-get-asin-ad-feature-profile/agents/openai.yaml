interface:
  display_name: "SIF 功能：ads_get_asin_ad_window_feature_profile 的兼容别名，执行逻辑完全相同。"
  short_description: "调用 SIF standard API：功能：ads_get_asin_ad_window_feature_profile 的兼"
  default_prompt: "Use $wenmai-sif-ads-get-asin-ad-feature-profile to call ads_get_asin_ad_feature_profile and return traceable results."
policy:
  allow_implicit_invocation: true
