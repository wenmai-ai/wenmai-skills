interface:
  display_name: "SIF 功能：基于 ASIN 广告数据，生成指定时间窗口内的广告特征画像，描述窗口期内的结构、集中度、投放节奏和稳定性。"
  short_description: "调用 SIF standard API：功能：基于 ASIN 广告数据，生成指定时间窗口内的广告特征画像，描述窗口期内的结构、集"
  default_prompt: "Use $wenmai-sif-ads-get-asin-ad-window-feature-profile to call ads_get_asin_ad_window_feature_profile and return traceable results."
policy:
  allow_implicit_invocation: true
