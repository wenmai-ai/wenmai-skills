interface:
  display_name: "SIF 需求判断层——回答'这个词的需求处于什么生命周期阶段，是在增长、在萎缩还是只是季节性低谷，以及现在是进场、加速、收割还是收缩的时机'。"
  short_description: "调用 SIF standard API：需求判断层——回答'这个词的需求处于什么生命周期阶段，是在增长、在萎缩还是只是季节性低谷"
  default_prompt: "Use $wenmai-sif-market-get-keyword-demand to call market_get_keyword_demand and return traceable results."
policy:
  allow_implicit_invocation: true
