interface:
  display_name: "SIF 需求边界层——回答'这个词背后的整个市场有多大，买家需求是集中在精确词上，还是分散在大量长尾变体词里'。"
  short_description: "调用 SIF standard API：需求边界层——回答'这个词背后的整个市场有多大，买家需求是集中在精确词上，还是分散在大量"
  default_prompt: "Use $wenmai-sif-market-get-keyword-root-trend to call market_get_keyword_root_trend and return traceable results."
policy:
  allow_implicit_invocation: true
