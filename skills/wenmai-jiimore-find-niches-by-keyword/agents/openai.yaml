interface:
  display_name: "JIIMORE 按关键词查询细分市场"
  short_description: "调用 JIIMORE 标准 API：按关键词查询细分市场"
  default_prompt: "Use $wenmai-jiimore-find-niches-by-keyword to 按关键词查询细分市场 and return traceable results."
policy:
  allow_implicit_invocation: true
