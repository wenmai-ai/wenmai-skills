interface:
  display_name: "SIF 功能：查询某 campaign 在单个自然周内的贡献明细，支持按 keyword 或 ad_group 维度拆解。"
  short_description: "调用 SIF standard API：功能：查询某 campaign 在单个自然周内的贡献明细，支持按 keyword 或 a"
  default_prompt: "Use $wenmai-sif-ads-get-campaign-contribution-breakdown to call ads_get_campaign_contribution_breakdown and return traceable results."
policy:
  allow_implicit_invocation: true
