interface:
  display_name: "稳卖 Alpha Target Scraper"
  short_description: "调用 Alpha Target Scraper API"
  default_prompt: "Use $wenmai-alpha-target-scraper to call the Wenmai Alpha `alpha_target_scraper` standard API."
policy:
  allow_implicit_invocation: true
