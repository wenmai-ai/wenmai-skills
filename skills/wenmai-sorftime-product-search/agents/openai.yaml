interface:
  display_name: "Sorftime Amazon（亚马逊） 选产品（实时）"
  short_description: "调用 Sorftime Amazon（亚马逊） standard API：选产品（实时）"
  default_prompt: "Use $wenmai-sorftime-product-search to call product_search and return traceable results."
policy:
  allow_implicit_invocation: true
