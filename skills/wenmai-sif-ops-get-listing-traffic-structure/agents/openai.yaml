interface:
  display_name: "SIF 功能：查看 Listing 内各变体的流量结构拆解，返回每个变体在自然流量、SP、SB、SBV 渠道中各自的分数与占比。"
  short_description: "调用 SIF standard API：功能：查看 Listing 内各变体的流量结构拆解，返回每个变体在自然流量、SP、SB、"
  default_prompt: "Use $wenmai-sif-ops-get-listing-traffic-structure to call ops_get_listing_traffic_structure and return traceable results."
policy:
  allow_implicit_invocation: true
