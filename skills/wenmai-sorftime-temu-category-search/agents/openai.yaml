interface:
  display_name: "Sorftime 选类目"
  short_description: "调用 Sorftime standard API：选类目"
  default_prompt: "Use $wenmai-sorftime-temu-category-search to call temu_category_search and return traceable results."
policy:
  allow_implicit_invocation: true
