interface:
  display_name: "稳卖 XYDC Keyword ABA Trends"
  short_description: "调用 XYDC 关键词ABA数据趋势（周） API"
  default_prompt: "Use $wenmai-xydc-keyword-aba-trends to call the Wenmai XYDC `get_keyword_aba_trends` standard API."
policy:
  allow_implicit_invocation: true
