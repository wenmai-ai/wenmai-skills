interface:
  display_name: "SIF 功能：基于曝光得分，查询某 ASIN 在指定时间窗口内各 campaign 的贡献总览，按贡献从高到低排序。"
  short_description: "调用 SIF standard API：功能：基于曝光得分，查询某 ASIN 在指定时间窗口内各 campaign 的贡献总览，"
  default_prompt: "Use $wenmai-sif-ads-get-asin-campaign-contribution-overview to call ads_get_asin_campaign_contribution_overview and return traceable results."
policy:
  allow_implicit_invocation: true
