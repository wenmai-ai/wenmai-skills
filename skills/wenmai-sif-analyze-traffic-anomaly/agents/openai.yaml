interface:
  display_name: "SIF 所有流量变化分析的主入口工具。功能：对 ASIN 进行端到端的流量下跌根因分析——自动识别异常窗口，从广告侧、自然侧、关键词侧逐层拆因。注意：判断逻辑仍在持续迭"
  short_description: "调用 SIF standard API：所有流量变化分析的主入口工具。功能：对 ASIN 进行端到端的流量下跌根因分析——自动识"
  default_prompt: "Use $wenmai-sif-analyze-traffic-anomaly to call analyze_traffic_anomaly and return traceable results."
policy:
  allow_implicit_invocation: true
