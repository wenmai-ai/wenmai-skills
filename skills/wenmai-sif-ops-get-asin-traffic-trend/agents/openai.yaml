interface:
  display_name: "SIF 功能：查看 ASIN 的流量趋势时间序列，按周期返回总流量分数及自然/广告渠道拆解。"
  short_description: "调用 SIF standard API：功能：查看 ASIN 的流量趋势时间序列，按周期返回总流量分数及自然/广告渠道拆解。"
  default_prompt: "Use $wenmai-sif-ops-get-asin-traffic-trend to call ops_get_asin_traffic_trend and return traceable results."
policy:
  allow_implicit_invocation: true
