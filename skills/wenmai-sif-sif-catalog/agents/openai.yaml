interface:
  display_name: "SIF 返回 SIF 可用接口的分类目录。触发时机：用户询问'有哪些工具' / '能做什么' / '功能列表' / '工具介绍' 时使用。返回内容按大类组织"
  short_description: "调用 SIF standard API：返回 SIF 可用接口的分类目录。触发时机：用户询问'有哪些工具' / '能做什么' /"
  default_prompt: "Use $wenmai-sif-sif-catalog to call sif_catalog and return traceable results."
policy:
  allow_implicit_invocation: true
