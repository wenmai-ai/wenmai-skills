interface:
  display_name: "SIF 功能：查询某 ASIN 历史全量的广告流量趋势，按 SP/SB/SBV 三个渠道分别输出曝光量时序。"
  short_description: "调用 SIF standard API：功能：查询某 ASIN 历史全量的广告流量趋势，按 SP/SB/SBV 三个渠道分别输出"
  default_prompt: "Use $wenmai-sif-ads-get-asin-ad-traffic-trend to call ads_get_asin_ad_traffic_trend and return traceable results."
policy:
  allow_implicit_invocation: true
