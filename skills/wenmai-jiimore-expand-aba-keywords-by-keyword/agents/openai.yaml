interface:
  display_name: "JIIMORE 按关键词扩展 ABA 关键词"
  short_description: "调用 JIIMORE 标准 API：按关键词扩展 ABA 关键词"
  default_prompt: "Use $wenmai-jiimore-expand-aba-keywords-by-keyword to 按关键词扩展 ABA 关键词 and return traceable results."
policy:
  allow_implicit_invocation: true
