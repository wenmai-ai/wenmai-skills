interface:
  display_name: "SIF 功能：查询单个广告组从创建至今的完整历史流量趋势，可附带用户选中窗口的上下文便于对比定位。"
  short_description: "调用 SIF standard API：功能：查询单个广告组从创建至今的完整历史流量趋势，可附带用户选中窗口的上下文便于对比定位"
  default_prompt: "Use $wenmai-sif-ads-get-ad-group-traffic-trend to call ads_get_ad_group_traffic_trend and return traceable results."
policy:
  allow_implicit_invocation: true
