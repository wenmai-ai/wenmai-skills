interface:
  display_name: "SellerSprite 用于在指定 Amazon 站点和时间点（按周），"
  short_description: "调用 SellerSprite standard API：用于在指定 Amazon 站点和时间点（按周），"
  default_prompt: "Use $wenmai-sellersprite-aba-research-weekly to call aba_research_weekly and return traceable results."
policy:
  allow_implicit_invocation: true
