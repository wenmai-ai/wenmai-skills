interface:
  display_name: "SIF 功能：查询单个广告组在指定周的关键词明细，包含每个关键词的流量占比以及该关键词在哪些 ASIN 上展示。"
  short_description: "调用 SIF standard API：功能：查询单个广告组在指定周的关键词明细，包含每个关键词的流量占比以及该关键词在哪些 A"
  default_prompt: "Use $wenmai-sif-ads-get-ad-group-keyword-breakdown to call ads_get_ad_group_keyword_breakdown and return traceable results."
policy:
  allow_implicit_invocation: true
