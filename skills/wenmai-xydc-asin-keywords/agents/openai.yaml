interface:
  display_name: "稳卖 XYDC ASIN Keywords"
  short_description: "调用 XYDC asin反查关键词列表（最近天） API"
  default_prompt: "Use $wenmai-xydc-asin-keywords to call the Wenmai XYDC `get_asin_keywords` standard API."
policy:
  allow_implicit_invocation: true
