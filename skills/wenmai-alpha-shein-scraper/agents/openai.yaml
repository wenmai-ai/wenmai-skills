interface:
  display_name: "稳卖 Alpha Shein Scraper"
  short_description: "调用 Alpha Shein Scraper API"
  default_prompt: "Use $wenmai-alpha-shein-scraper to call the Wenmai Alpha `alpha_shein_scraper` standard API."
policy:
  allow_implicit_invocation: true
