interface:
  display_name: "稳卖 SellerSprite traffic keyword stat"
  short_description: "用于分析 Amazon ASIN 的流量关键词结构概览统计"
  default_prompt: "Use $wenmai-sellersprite-traffic-keyword-stat to call the Wenmai SellerSprite `traffic_keyword_stat` standard API."
policy:
  allow_implicit_invocation: true
