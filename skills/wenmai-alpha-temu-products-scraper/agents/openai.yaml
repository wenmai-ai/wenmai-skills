interface:
  display_name: "稳卖 Alpha Temu Products Scraper"
  short_description: "调用 Alpha Temu Products Scraper API"
  default_prompt: "Use $wenmai-alpha-temu-products-scraper to call the Wenmai Alpha `alpha_temu_products_scraper` standard API."
policy:
  allow_implicit_invocation: true
