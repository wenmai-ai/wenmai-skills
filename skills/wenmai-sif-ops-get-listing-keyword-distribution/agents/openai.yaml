interface:
  display_name: "SIF 功能：查看各变体的关键词数量分布，返回每个变体在自然流量、SP、SB、SBV 各渠道中覆盖的流量词数量。"
  short_description: "调用 SIF standard API：功能：查看各变体的关键词数量分布，返回每个变体在自然流量、SP、SB、SBV 各渠道中覆"
  default_prompt: "Use $wenmai-sif-ops-get-listing-keyword-distribution to call ops_get_listing_keyword_distribution and return traceable results."
policy:
  allow_implicit_invocation: true
