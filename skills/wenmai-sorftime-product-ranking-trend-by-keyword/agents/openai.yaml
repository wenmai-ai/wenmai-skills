interface:
  display_name: "Sorftime 产品关键词排名趋势"
  short_description: "调用 Sorftime standard API：产品关键词排名趋势"
  default_prompt: "Use $wenmai-sorftime-product-ranking-trend-by-keyword to call product_ranking_trend_by_keyword and return traceable results."
policy:
  allow_implicit_invocation: true
