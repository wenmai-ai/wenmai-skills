interface:
  display_name: "Sorftime 选产品"
  short_description: "调用 Sorftime standard API：选产品"
  default_prompt: "Use $wenmai-sorftime-temu-product-search to call temu_product_search and return traceable results."
policy:
  allow_implicit_invocation: true
