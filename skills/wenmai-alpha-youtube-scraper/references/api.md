# Wenmai Alpha Youtube Scraper API 参考

## 调用规范

- **请求地址**：`${WENMAI_API_ORIGIN:-https://all-api.wenmai-ai.com}/wmapi/v1/alpha/youtube-scraper`
- **请求方式**：POST，`Content-Type: application/json`
- **认证方式**：Header `secret-key: $WENMAI_API_KEY`，也兼容 `WENMAI_SECRET_KEY`；secret-key 获取与充值指引见 https://skill.wenmai-ai.com/wenmaiskills/use_guide.html。
- **接口编码**：`alpha_youtube_scraper`
- **接口说明**：Youtube Scraper
- **脚本入口**：`scripts/alpha_youtube_scraper.py`，脚本参数即标准 API POST Body JSON

### 运行时覆盖

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `WENMAI_API_ORIGIN` | Wenmai API 地址 | `https://all-api.wenmai-ai.com` |
| `WENMAI_API_BASE_PATH` | 标准 API Base Path | `/wmapi/v1` |
| `WENMAI_API_TIMEOUT` | HTTP 超时时间，单位秒 | `120` |

## 请求参数

POST Body（JSON）：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `searchQueries` | array | 否 | 搜索关键词列表。 |
| `maxResults` | integer | 否 | 最大采集或返回数量。 |
| `maxResultsShorts` | integer | 否 | 最大采集或返回数量。 |
| `maxResultStreams` | integer | 否 | 最大采集或返回数量。 |
| `startUrls` | array | 否 | 页面链接列表。 |
| `downloadSubtitles` | boolean | 否 | 是否启用该选项。 |
| `subtitlesLanguage` | string | 否 | 语言代码。可选值：any、en、de、es、fr、it、ja、ko、nl、pt、ru。 |
| `subtitlesFormat` | string | 否 | subtitlesFormat 字段。可选值：srt、vtt、xml、plaintext。 |
| `sortingOrder` | string | 否 | 排序方式。可选值：relevance、rating、date、views。 |
| `dateFilter` | string | 否 | 时间或日期。可选值：hour、today、week、month、year。 |
| `videoType` | string | 否 | videoType 字段。可选值：video、movie。 |
| `lengthFilter` | string | 否 | lengthFilter 字段。可选值：under4、between420、plus20。 |
| `isHD` | boolean | 否 | 是否启用该选项。 |
| `hasSubtitles` | boolean | 否 | 是否启用该选项。 |
| `hasCC` | boolean | 否 | 是否启用该选项。 |
| `is3D` | boolean | 否 | 是否启用该选项。 |
| `isLive` | boolean | 否 | 是否启用该选项。 |
| `isBought` | boolean | 否 | 是否启用该选项。 |
| `is4K` | boolean | 否 | 是否启用该选项。 |
| `is360` | boolean | 否 | 是否启用该选项。 |
| `hasLocation` | boolean | 否 | 是否启用该选项。 |
| `isHDR` | boolean | 否 | 是否启用该选项。 |
| `isVR180` | boolean | 否 | 是否启用该选项。 |
| `oldestPostDate` | string | 否 | 频道内容的最早发布日期。传入该参数会启用单独计费的频道日期范围能力；仅在用户明确要求时使用。 |
| `sortVideosBy` | string | 否 | 排序方式。可选值：NEWEST、POPULAR、OLDEST。 |
| `saveSubsToKVS` | boolean | 否 | saveSubsToKVS 字段。 |
| `preferAutoGeneratedSubtitles` | boolean | 否 | preferAutoGeneratedSubtitles 字段。 |

### 额外计费提醒

- `oldestPostDate` 会启用单独计费的频道日期范围能力。普通请求应省略该参数。
- 当前接口参数表未提供 AI 描述或 AI 摘要开关，不要构造未记录的参数。
- 不向用户展示或解释上游价格、套餐、阶梯或计费公式。

## 请求示例

脚本入参示例：

```json
{
  "is3D": false,
  "is4K": false,
  "isHD": false,
  "hasCC": false,
  "is360": false,
  "isHDR": false,
  "isLive": false,
  "isVR180": false,
  "isBought": false,
  "videoType": "video",
  "dateFilter": "month",
  "maxResults": 3,
  "hasLocation": false,
  "hasSubtitles": false,
  "lengthFilter": "between420",
  "sortVideosBy": "NEWEST",
  "sortingOrder": "relevance",
  "saveSubsToKVS": false,
  "searchQueries": [
    "Crawlee"
  ],
  "subtitlesFormat": "srt",
  "maxResultStreams": 0,
  "maxResultsShorts": 0,
  "downloadSubtitles": false,
  "subtitlesLanguage": "en",
  "preferAutoGeneratedSubtitles": false
}
```

## 响应结构

Wenmai 标准 API 返回统一响应。成功时 `code` 为 `OK`，业务数据位于 `data`；失败时 `data` 通常为 `null`，错误信息位于 `message`。ALPHA 采集类接口可能同步返回业务字段，也可能返回任务状态、数据集记录或分页字段；以下为该接口文档列出的业务字段。

| 字段 | 类型 | 说明 |
|------|------|------|
| `title` | string | 标题。 |
| `id` | string | ID 标识。 |
| `url` | string | 链接地址。 |
| `viewCount` | number | 数量。 |
| `date` | string | 时间或日期。 |
| `duration` | string | duration 字段。 |
| `type` | string | 采集模式。 |
| `thumbnailUrl` | string | 链接地址。 |
| `channelName` | string | channelName 字段。 |
| `channelUrl` | string | 链接地址。 |
| `channelUsername` | string | 作者信息。 |
| `translatedTitle` | string | translatedTitle 字段。 |
| `translatedText` | string | translatedText 字段。 |
| `likes` | number | likes 字段。 |
| `location` | string | location 字段。 |
| `channelId` | string | ID 标识。 |
| `commentsCount` | number | 评论列表。 |
| `text` | string | 文本内容。 |
| `descriptionLinks` | array | 描述。 |
| `descriptionLinks.text` | string | 文本内容。 |
| `descriptionLinks.url` | string | 链接地址。 |
| `subtitles` | array | subtitles 字段。 |
| `subtitles.srtUrl` | string | 链接地址。 |
| `subtitles.type` | string | 采集模式。 |
| `subtitles.language` | string | 语言代码。 |
| `subtitles.srt` | string | srt 字段。 |
| `fromYTUrl` | string | 链接地址。 |
| `order` | number | 排序方式。 |
| `commentsTurnedOff` | boolean | 评论列表。 |
| `isMonetized` | boolean | 是否启用该选项。 |
| `hashtags` | array<string> | 搜索关键词列表。 |
| `isMembersOnly` | boolean | 是否启用该选项。 |
| `collaborators` | array | collaborators 字段。 |
| `collaborators.name` | string | 名称。 |
| `collaborators.username` | string | 作者信息。 |
| `collaborators.url` | string | 链接地址。 |
| `isPaidContent` | boolean | 是否启用该选项。 |
| `numberOfSubscribers` | number | 数量。 |
| `channelTotalVideos` | number | 数量。 |
| `channelDescription` | string | 描述。 |
| `channelLocation` | string | channelLocation 字段。 |
| `channelJoinedDate` | string | 时间或日期。 |
| `channelTotalViews` | number | 数量。 |
| `isChannelVerified` | boolean | 是否启用该选项。 |
| `channelBannerUrl` | string | 链接地址。 |
| `channelAvatarUrl` | string | 链接地址。 |
| `isAgeRestricted` | boolean | 是否启用该选项。 |

## 使用要点

- 本接口适合：YouTube 视频、频道、搜索或评论。
- 优先使用用户给定的 URL、关键词、商品 ID、站点、国家、语言、排序、分页和数量限制。
- 采集类接口的字段会随目标平台页面结构变化；输出分析时保留原始字段名和 URL，避免把缺失字段补写成事实。
- 当用户需要多平台对比时，分别调用对应 Alpha 原子 Skill，再在上层分析中合并结果。

## 错误处理

| 场景 | 处理建议 |
|------|----------|
| 缺少 API Key | 参考 https://skill.wenmai-ai.com/wenmaiskills/use_guide.html 获取 secret-key，并设置为 `WENMAI_API_KEY`（或 `WENMAI_SECRET_KEY`）；不要把 Key 写进 Skill 文件或对话。 |
| 余额或额度不足 | 参考 https://skill.wenmai-ai.com/wenmaiskills/use_guide.html 完成充值后重试。 |
| 参数错误 | 按上方请求参数表修正枚举值、日期格式、分页范围、URL/关键词数组、数量范围或必填字段。 |
| 非 OK 响应 | 读取响应 `message` 与 `requestId`，按接口文档或联系网关排查。 |

## curl 示例

```bash
curl -sS -X POST \
  "${WENMAI_API_ORIGIN:-https://all-api.wenmai-ai.com}/wmapi/v1/alpha/youtube-scraper" \
  -H "secret-key: $WENMAI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"is3D": false, "is4K": false, "isHD": false, "hasCC": false, "is360": false, "isHDR": false, "isLive": false, "isVR180": false, "isBought": false, "videoType": "video", "dateFilter": "month", "maxResults": 3, "hasLocation": false, "hasSubtitles": false, "lengthFilter": "between420", "sortVideosBy": "NEWEST", "sortingOrder": "relevance", "saveSubsToKVS": false, "searchQueries": ["Crawlee"], "subtitlesFormat": "srt", "maxResultStreams": 0, "maxResultsShorts": 0, "downloadSubtitles": false, "subtitlesLanguage": "en", "preferAutoGeneratedSubtitles": false}'
```

---

来源：Wenmai WMAPI 文档；额外计费参数提醒依据《新数据中心供应商价格表》（核对日期：2026-07-24）。
