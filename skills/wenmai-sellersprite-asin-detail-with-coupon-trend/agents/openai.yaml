interface:
  display_name: "稳卖 SellerSprite asin detail with coupon trend"
  short_description: "查询指定 ASIN 在 Amazon 指定市场下的完整商品详情信息，"
  default_prompt: "Use $wenmai-sellersprite-asin-detail-with-coupon-trend to call the Wenmai SellerSprite `asin_detail_with_coupon_trend` standard API."
policy:
  allow_implicit_invocation: true
